<% if !logged_in? %>
  Login with Facebook
  <%= link_to "Dead link" %><br>

  <%= link_to "User Survey", new_path %>
<% else %>
  <% form_for(@recommendation) do |f| %>
  
  <%= f.submit "Recommend!" %>
<% end %>
